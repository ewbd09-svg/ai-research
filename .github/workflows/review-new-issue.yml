name: Review New Issue Submissions
on:
  issues:
    types: [opened]
  push:
    branches: [master]
permissions:
  issues: write
  contents: write
jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - run: echo test
